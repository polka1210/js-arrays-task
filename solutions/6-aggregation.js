// BEGIN
const calculateSum = (numbers) => {
    let sum = 0;
    for (let x = 0; x < numbers.length; x++) {
        const num = numbers[x];
       if (num % 2 !== 0 num < 20) {
        sum += num; 
    }
}
return sum;
};
export { calculateSum };
// END